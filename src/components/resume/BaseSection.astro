---
type Props = {
  heading: string;
  class?: string;
};

const { heading, class: className } = Astro.props;
---

<section class:list={["mt-6", className]}>
  <h2 class="mb-4 font-600 text-xs text-slate-500 uppercase tracking-[0.2em]">{heading}</h2>
  <slot />
</section>
